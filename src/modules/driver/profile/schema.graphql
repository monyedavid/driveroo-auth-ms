type driver_Response {
    ok: Boolean!
    error: [Error!]
    success: Driver
}

type Mutation {
    firstUpdate(params: up_Driver_Params!, mock: MockSession!): driver_Response!
}

input MockSession {
    id: ID!
    token: String!
    driversExt: String!
    avatarExt: String!
}
